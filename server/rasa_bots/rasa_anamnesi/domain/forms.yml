version: "3.1"
forms:
  # name_form:
  #   required_slots:

  # symptoms_form:
  #   required_slots:

  investigate_form:
    required_slots:
      - name
      # - symptoms_confirmation
      - symptoms
      - first_time
      - drink
      - smoke
      # - pregnant
      - medications
      - diseases
      - family_diseases
      - allergies
