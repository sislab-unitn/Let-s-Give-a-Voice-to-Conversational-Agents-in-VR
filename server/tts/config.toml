[server]
    self_host = "localhost"
    self_port = 8082
    self_SSL = false
[model]
    compile = false
    device = "cuda"
[audio]
    sample_rate = 24000 # 
    pause_length = 100 # pause length in between sentences, in samples (16000 samples per second, 100 samples = 0.00625 seconds)
    speaker_wav = 'speaker.wav'